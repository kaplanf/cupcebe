<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light">
        <item name="android:actionBarStyle">@style/MyAppActionBarTheme</item>
        <item name="android:actionMenuTextColor">@color/cuppcebe_blue</item>
        <item name="colorPrimary">@android:color/white</item>


    </style>

    <style name="MyAppActionBarTheme" parent="android:Theme.Holo.Light">
        <item name="android:actionBarStyle">@style/MyApp.ActionBar</item>
    </style>


    <!-- Bonus: if you want to style whole Action Bar, not just the menu -->
    <style name="MyApp.ActionBar" parent="android:Widget.Holo.Light.ActionBar.Solid">
        <item name="android:background">@android:color/white</item>
    </style>

    <style name="MenuItemText">
        <item name="android:textColor">@color/cuppcebe_blue</item>
    </style>

    <style name="ActionBarWidget" parent="@android:style/Theme.Holo">
        <item name="android:itemTextAppearance">@style/MenuItemText</item>
    </style>

    <style name="ProgressDialogStyle" parent="@android:style/Theme.Dialog">
        <item name="android:windowFrame">@null</item>
        <item name="android:windowBackground">@android:color/transparent</item>
        <item name="android:windowIsFloating">true</item>
        <item name="android:windowContentOverlay">@null</item>
        <item name="android:windowTitleStyle">@null</item>
        <item name="android:windowAnimationStyle">@android:style/Animation.Dialog</item>
        <item name="android:windowSoftInputMode">stateUnspecified|adjustPan</item>
        <item name="android:backgroundDimEnabled">false</item>
        <item name="android:background">@android:color/transparent</item>
    </style>

</resources>
